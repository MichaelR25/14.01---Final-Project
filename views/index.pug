extends layout

block content
  nav.nav-bar
    button Home 
    button Menu 
    img(src='./images/black_logo.png')
    button About Us 
    button Reviews
  div.page-content
    ul
      each product in featuredProducts
        li.product-display
          p= product.product_name + " - $" + product.price

